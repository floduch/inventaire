<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="inventaire.materiel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mat_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mat_foreground"
        android:supportsRtl="true"
        android:theme="@style/ThemeInventaire">
        <activity android:name=".Connexion"></activity>
        <activity android:name=".Administration" />
        <activity android:name=".MainActivity5" />
        <activity android:name=".MainActivity4" />
        <activity android:name=".MainActivity3" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>